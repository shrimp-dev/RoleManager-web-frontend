import styled from "styled-components";

export const Title = styled.div`
    width: 100%;

    hr{
        width: 85%;
        background: var(--dark);
        height: 2px;
        border-radius: 4px;
        border: none;
        margin: auto;
    }
`;
